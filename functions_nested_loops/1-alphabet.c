#include <stdio.h>
#include "main.h"
/**
 * print_alphabet - this is showing the alphabet
 */


void print_alphabet(void)
	{

	char lower = 'a';

	for (lower = 'a'; lower <= 'z'; lower++)
	_putchar(lower);

	_putchar('\n');

	}

