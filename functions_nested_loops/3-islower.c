#include <stdio.h>
#include "main.h"
/**
 * _islower - checks for a lowercase character
 *
 * @c: function
 *
 * Return: thing
 */
	int _islower(int c)

	{

	int n = 'c';

	if (n == c)
	{
	return (1);
	}
	else
	{
	return (0);
	}

	}
